version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies...
      - npm install

  build:
    commands:
      - echo Build step passed. No build required for plain Node.js

artifacts:
  files:
    - '**/*'
  discard-paths: yes
